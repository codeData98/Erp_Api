<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
  位置约定：与 Mapper 接口保持相同的包路径
    Java : src/main/java/com/erp/signoff/mapper/PsSignMMapper.java
    XML  : src/main/resources/com/erp/signoff/mapper/PsSignMMapper.xml
  编译后二者都会落到 target/classes/com/erp/signoff/mapper/ 下，
  MyBatis 按 "namespace 全限定名 + .xml" 的约定自动加载，无需配置 mapper-locations。
-->
<mapper namespace="com.erp.signoff.mapper.PsSignSMapper">

    <insert id="insertBatch">
        INSERT INTO ps_sign_s (
        org_id, header_id, line_id, size_no, size_seq,
        sign_qty, cre_user, cre_date, last_user, last_date
        ) VALUES
        <foreach collection="psSignSList" item="item" separator=",">
            (
            #{item.orgId}, #{item.headerId}, #{item.lineId}, #{item.sizeNo}, #{item.sizeSeq},
            #{item.signQty}, #{item.creUser}, #{item.creDate}, #{item.lastUser}, #{item.lastDate}
            )
        </foreach>
    </insert>

</mapper>
